import React from 'react';
import './index.css';
import { Grid } from '@material-ui/core';

const Player = () => {

    

    return(
        <Grid item xs={8}>
            Player
        </Grid>
    );
}

export default Player;